format: conda
distro: conda
distro_dirs:
- anaconda
versions:
- "latest"
repos:
  conda-forge:
  pytorch:
  MindSpore:
index_url: $mirror/cloud/$repo/$conda_arch/$conda_repofile
amend_index_urls:
  noarch: $mirror/cloud/$repo/noarch/$conda_repofile

# Example URLs:
# https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/linux-64/current_repodata.json.gz
