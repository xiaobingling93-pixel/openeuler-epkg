format: conda
distro: anaconda
versions: # Latest release
- "latest"
repos:
  main:
  free:
  r:
index_url: $mirror/pkgs/$repo/linux-$arch/repodata.json.zst

# Example URLs:
# https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/linux-aarch64/repodata.json.zst
