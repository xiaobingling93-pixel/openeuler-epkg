format: deb
distro: debian
distro_dirs:
- postgresql
versions:
- bookworm  12
- bullseye  11
- buster    10
repos:
  postgresql:
  # 18:  # To select a specific version, uncomment this line and comment out the above "postgresql:" line
  # 17:
  # 16:
index_url: https://mirrors.tuna.tsinghua.edu.cn///postgresql/repos/apt/dists/$version-pgdg/Release

# Example URLs:
# https://mirrors.tuna.tsinghua.edu.cn/postgresql/repos/apt/dists/bookworm-pgdg/Release
